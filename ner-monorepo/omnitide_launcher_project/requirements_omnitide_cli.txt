# Requirements for the Omnitide CLI Toolkit (omnitide_cli.py, scribe.py, exworkagent.py)
# Generated by Drake v0.2

typer[all]>=0.9.0,<1.0.0
requests>=2.25.0,<3.0.0
# For ExWork's ENCRYPT_DECRYPT_TARGET handler
pycryptodomex>=3.10.0,<4.0.0
# For Scribe's LLM communication (preferred if available)
httpx>=0.23.0,<1.0.0
# For Scribe's .scribe.toml parsing (if Python < 3.11, though setup targets 3.11)
# Scribe itself handles tomllib for Python 3.11+ internally.
# Keeping tomli here for completeness if a project venv somehow ends up < 3.11,
# but it's less critical given the python3.11 focus.
tomli>=1.0.0,<3.0.0; python_version < "3.11"

# Note: External CLI tools required by agents (not installed via pip by this file):
# - For ExWork: ruff, nmap, sysctl (Linux), modprobe (Linux), ip (Linux, for ip route)
# - For Scribe: ruff, mypy, pytest, git, pip-audit (Scribe attempts to install pip-audit in venv)
